{
  "buildCommand": "npm --prefix apps/web run build",
  "installCommand": "npm install --no-audit --no-fund && npm --prefix apps/web install --no-audit --no-fund",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next"
}
